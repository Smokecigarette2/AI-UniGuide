<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <title>AI-UniGuide – DataHub вузов РК</title>
  <style>
    * {
      box-sizing: border-box;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    body {
      margin: 0;
      padding: 0;
      background: #0f172a;
      color: #e5e7eb;
      display: flex;
      justify-content: center;
    }

    .page {
      width: 100%;
      max-width: 1100px;
      padding: 24px;
    }

    header {
      margin-bottom: 16px;
    }

    .logo {
      font-weight: 700;
      font-size: 26px;
      letter-spacing: 0.06em;
    }

    .tagline {
      font-size: 13px;
      color: #9ca3af;
      margin-top: 4px;
    }


    .logo-link {
  text-decoration: none;
  color: inherit;
  display: inline-block;
}

.logo-link:hover .logo {
  opacity: 0.9;
}

    /* ---------- HERO / СЛАЙДЕР ---------- */

    .hero {
      margin-top: 20px;
      padding: 20px;
      background: #111827;
      border-radius: 16px;
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .hero-arrow {
      width: 40px;
      height: 40px;
      border-radius: 999px;
      border: none;
      background: #1f2937;
      color: #e5e7eb;
      font-size: 20px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: 0.15s ease;
    }

    .hero-arrow:hover {
      background: #374151;
      transform: translateY(-1px);
    }

    .hero-card {
      flex: 1;
      background: #020617;
      border-radius: 14px;
      padding: 18px 20px;
      border: 1px solid #1f2937;
      min-height: 120px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .hero-title {
      font-size: 22px;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .hero-meta {
      font-size: 13px;
      color: #9ca3af;
      margin-bottom: 10px;
    }

    .hero-desc {
      font-size: 14px;
      color: #d1d5db;
      margin-bottom: 10px;
    }

    .hero-actions {
      margin-top: 8px;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .btn-primary {
      border: none;
      border-radius: 999px;
      padding: 8px 18px;
      background: #3b82f6;
      color: #fff;
      font-weight: 600;
      font-size: 13px;
      cursor: pointer;
      transition: 0.15s ease;
    }

    .btn-primary:hover {
      background: #2563eb;
      transform: translateY(-1px);
    }

    .btn-ghost {
      border-radius: 999px;
      border: 1px solid #374151;
      background: transparent;
      color: #e5e7eb;
      padding: 8px 16px;
      font-size: 13px;
      cursor: pointer;
      transition: 0.15s ease;
    }

    .btn-ghost:hover {
      background: #111827;
    }

    /* ---------- ПОИСК / AI ---------- */

    .search-block {
      margin-top: 18px;
      padding: 14px 16px;
      background: #020617;
      border-radius: 12px;
      border: 1px solid #1f2937;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .search-label {
      font-size: 13px;
      color: #9ca3af;
    }

    .search-row {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .search-input {
      flex: 1;
      min-width: 200px;
      border-radius: 999px;
      border: 1px solid #374151;
      background: #020617;
      color: #e5e7eb;
      padding: 10px 14px;
      font-size: 14px;
    }

    .search-input::placeholder {
      color: #6b7280;
    }

    .ai-hint {
      font-size: 11px;
      color: #6b7280;
    }

    .ai-result {
      margin-top: 6px;
      font-size: 13px;
      color: #d1d5db;
      min-height: 18px;
    }

    /* ---------- НИЖНИЙ БЛОК: ПРИВЕТСТВИЕ + НОВОСТИ ---------- */

    .bottom-row {
      margin-top: 24px;
      display: grid;
      grid-template-columns: minmax(0, 2fr) minmax(0, 1.2fr);
      gap: 18px;
    }

    .welcome {
      background: #111827;
      border-radius: 14px;
      padding: 18px 20px;
    }

    .welcome h2 {
      margin: 0 0 10px 0;
      font-size: 20px;
    }

    .welcome p {
      margin: 0 0 6px 0;
      font-size: 14px;
      color: #d1d5db;
    }

    .welcome ul {
      margin: 8px 0 0 18px;
      padding: 0;
      font-size: 13px;
      color: #9ca3af;
    }

    .news {
      background: #111827;
      border-radius: 14px;
      padding: 18px 20px;
    }

    .news h2 {
      margin: 0 0 10px 0;
      font-size: 18px;
    }

    .news-item {
      margin-bottom: 10px;
      padding-bottom: 8px;
      border-bottom: 1px solid #1f2937;
    }

    .news-item:last-child {
      border-bottom: none;
      margin-bottom: 0;
      padding-bottom: 0;
    }

    .news-title {
      font-size: 13px;
      color: #e5e7eb;
    }

    .news-date {
      font-size: 11px;
      color: #6b7280;
      margin-top: 2px;
    }

    @media (max-width: 800px) {
      .bottom-row {
        grid-template-columns: 1fr;
      }

      .hero {
        flex-direction: column;
      }

      .hero-arrow {
        order: -1;
      }
    }
  </style>
</head>
<body>
  <div class="page">
    <header>
  <a href="index.html" class="logo-link">
    <span class="logo">AI-UniGuide</span>
  </a>
  <div class="tagline">DataHub вузов РК с AI-помощником</div>
</header>


    <!-- HERO / СЛАЙДЕР ВУЗОВ -->
    <section class="hero">
      <button class="hero-arrow" id="prevUni">‹</button>

      <div class="hero-card">
        <div>
          <div class="hero-title" id="heroName">KIMEP</div>
          <div class="hero-meta" id="heroCity">Алматы · частный университет</div>
          <div class="hero-desc" id="heroDesc">
            Бизнес, экономика, международные отношения. Программы на английском языке,
            акцент на прикладном образовании.
          </div>
        </div>
        <div class="hero-actions">
          <button class="btn-primary" id="openUniBtn">
            Перейти к вузу (MVP)
          </button>
          <button class="btn-ghost" id="compareBtn">
            Сравнить с другим вузом
          </button>
        </div>
      </div>

      <button class="hero-arrow" id="nextUni">›</button>
    </section>

    <!-- ПОИСК / AI -->
    <section class="search-block">
      <div class="search-label" id="searchLabel">
        Спроси AI о выборе вуза или различиях между ними
      </div>
      <div class="search-row">
        <input
          id="searchInput"
          class="search-input"
          type="text"
          placeholder="Например: Хочу учиться на IT в Алматы на гранте"
        />
        <button class="btn-primary" id="searchBtn">Спросить AI</button>
      </div>
      <div class="ai-hint">
        В MVP мы только формируем вопрос и показываем, что отправим на backend.
        Позже здесь будет реальный ответ от AI-модели в облаке.
      </div>
      <div class="ai-result" id="aiResult"></div>
    </section>

    <!-- НИЖНИЙ БЛОК: ПРИВЕТСТВИЕ + НОВОСТИ -->
    <section class="bottom-row">
      <div class="welcome">
        <h2>Приветствуем в DataHub вузов РК!</h2>
        <p>
          AI-UniGuide — это прототип единого каталога университетов Казахстана
          с интеллектуальным помощником. Наша цель — упростить выбор вуза и
          программы для абитуриентов и студентов.
        </p>
        <p>В рамках MVP мы показываем, как может работать:</p>
        <ul>
          <li>каталог вузов с ключевой информацией;</li>
          <li>AI-поиск по данным о программах и условиях поступления;</li>
          <li>функция сравнения вузов на основе их характеристик;</li>
          <li>переход к “странице вуза” с более подробным AI-гидом.</li>
        </ul>
      </div>

      <aside class="news">
        <h2>Новости</h2>

        <div class="news-item">
          <div class="news-title">
            Определены проходные баллы ЕНТ для поступления в 2026 году
          </div>
          <div class="news-date">13 ноября 2025</div>
        </div>

        <div class="news-item">
          <div class="news-title">
            Казахстан и зарубежные вузы запускают новые обменные программы
          </div>
          <div class="news-date">5 ноября 2025</div>
        </div>

        <div class="news-item">
          <div class="news-title">
            Обновлены требования к грантам на IT-специальности
          </div>
          <div class="news-date">28 октября 2025</div>
        </div>
      </aside>
    </section>
  </div>

 <script>
  // --- ДАННЫЕ ПО ВУЗАМ (пока локальные, без БД) ---
  const universities = [
    {
      id: "kimep",
      name: "KIMEP",
      city: "Алматы · частный университет",
      desc:
        "Бизнес, экономика, международные отношения. Программы на английском языке, акцент на прикладном образовании."
    },
    {
      id: "narxoz",
      name: "NARXOZ",
      city: "Алматы · профильный по финансам и бизнесу",
      desc:
        "Экономика, финансы, бухгалтерский учет, государственное управление. Сильный фокус на прикладных кейсах."
    }
  ];

  let currentIndex = 0;

  const heroName = document.getElementById("heroName");
  const heroCity = document.getElementById("heroCity");
  const heroDesc = document.getElementById("heroDesc");
  const prevBtn = document.getElementById("prevUni");
  const nextBtn = document.getElementById("nextUni");
  const compareBtn = document.getElementById("compareBtn");
  const openUniBtn = document.getElementById("openUniBtn");

  const searchInput = document.getElementById("searchInput");
  const searchBtn = document.getElementById("searchBtn");
  const aiResult = document.getElementById("aiResult");

  function renderUni(index) {
    const uni = universities[index];
    heroName.textContent = uni.name;
    heroCity.textContent = uni.city;
    heroDesc.textContent = uni.desc;

    searchInput.placeholder =
      "Спроси AI про " +
      uni.name +
      " или сравнение с другим вузом (например: чем " +
      uni.name +
      " отличается от второго вуза?)";
  }

  prevBtn.addEventListener("click", () => {
    currentIndex = (currentIndex - 1 + universities.length) % universities.length;
    renderUni(currentIndex);
  });

  nextBtn.addEventListener("click", () => {
    currentIndex = (currentIndex + 1) % universities.length;
    renderUni(currentIndex);
  });

  compareBtn.addEventListener("click", async () => {
    const other =
      universities.length > 1
        ? universities[(currentIndex + 1) % universities.length].name
        : "другим вузом";

    const question =
      "Сравни, пожалуйста, " +
      universities[currentIndex].name +
      " и " +
      other +
      " по программам, стоимости и возможностям для студентов.";

    await sendToAI(question);
  });

  openUniBtn.addEventListener("click", () => {
    aiResult.textContent =
      "Здесь в будущем будет переход на страницу вуза " +
      universities[currentIndex].name +
      " с подробным AI-гидом.";
  });

  // --- КНОПКА "СПРОСИТЬ AI" ---
  searchBtn.addEventListener("click", async () => {
    const userText = searchInput.value.trim();
    const uni = universities[currentIndex];

    const finalQuestion =
      userText ||
      "Подскажи, чем университет " +
        uni.name +
        " подходит абитуриенту и как он отличается от других вузов в каталоге.";

    await sendToAI(finalQuestion, uni.id);
  });

  // --- ФУНКЦИЯ ОТПРАВКИ НА БЕКЕНД ---
  async function sendToAI(question, universityId) {
    aiResult.textContent = "AI думает...";

    try {
      const res = await fetch("http://127.0.0.1:8000/api/ask", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          question: question,
          universityId: universityId || null
        })
      });

      if (!res.ok) {
        aiResult.textContent = "Ошибка сервера: " + res.status;
        return;
      }

      const data = await res.json();
      aiResult.textContent = data.answer || "AI не ответил.";
    } catch (err) {
      console.error(err);
      aiResult.textContent = "Не удалось связаться с backend.";
    }
  }

  // начальный рендер
  renderUni(currentIndex);
</script>

</body>
</html>
